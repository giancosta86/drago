[package]
name = "drago"
version = "0.0.0"
publish = false
authors = ["Gianluca Costa <gianluca@gianlucacosta.info>"]
edition = "2021"
description = "Random sinogram generator for TypeScript"
repository = "https://github.com/giancosta86/drago"
license = "MIT"
keywords = ["random", "Chinese", "logograms", "TypeScript", "JavaScript"]

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = ["console_error_panic_hook"]

[dependencies]
chinese-rand = { version = "0.2.5", features = [
    "digit-sequence",
    "currency",
    "gregorian",
] }
chinese-format = { version = "0.9.3", features = [
    "digit-sequence",
    "currency",
    "gregorian",
] }
fastrand = "2.5.0"
serde = { version = "1.0.229", features = ["derive"] }
wasm-bindgen = "0.2.127"
tsify = { version = "0.5.6", features = ["json"] }
console_error_panic_hook = { version = "0.1.7", optional = true }

[dev-dependencies]
wasm-bindgen-test = "0.3.77"
pretty_assertions = "1.4.1"

[profile.release]
opt-level = "s"
lto = true
